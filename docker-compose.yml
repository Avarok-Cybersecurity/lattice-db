services:
  lattice-db:
    build: .
    ports:
      - "6334:6334"
    volumes:
      - lattice-data:/data
    environment:
      - LATTICE_DATA_DIR=/data
      - RUST_LOG=info
    restart: unless-stopped

volumes:
  lattice-data:
